---
title: GLOSSARY
type: reference
status: active
created: 2026-07-10
tags: [harness, glossary, ssot]
---

# GLOSSARY — naming authority

A name is decided here before its first use ([[adr-05-glossary-and-localisation]]). Every identifier-worthy term — model names, endpoint segments, env var stems, service names, UI labels, doc names — uses the canonical form below. A new term gets its row here first; the ABC gate ([[AGENTS]]) applies to naming like to everything else.

Format: Term | Canonical form | Applies to | Forbidden forms.

| Term | Canonical form | Applies to | Forbidden forms |
|---|---|---|---|
| integration branch | `main` | git default / PR target for features ([[GH]]) | `master` |
| production branch | `prod` | git production line; never call `main` production ([[GH]]) | `production`, `master`, `release` as the prod line |
| development flow | `issue → worktree → PR` | the mandatory shape of every change ([[adr-04-issue-delivery]], [[DEVELOPMENT-LOOP]]) | skipping the issue; a direct hand-commit to `main` |
| worktree | `worktree` — a git worktree, keyed to its issue, under `.claude/worktrees/`; optional, and destroyed on integration ([[adr-04-issue-delivery]] rule 5) | git isolation for a change | `checkout` (for this meaning); a worktree that outlives its PR |
| integration | `integration` — merge of a PR into `main`, performed only as the `gh`/kodexArg identity ([[adr-04-issue-delivery]], [[GH]]) | landing a change on `main` | calling a direct push to `main` "integration" |
| backend | `backend` | service name, ECR suffix, repo path `backend/`, docs | `server`, `api-server`, `api/` as the Django root path |
| frontend | `frontend` | service name, ECR suffix, repo path `frontend/`, docs | `web`, `client`, `ui`, `app/` as the Astro root path |
| environments | `dev`, `prod`, `local` | infra, secrets paths, docs | `staging` (does not exist here), `development`, `production` |
| adr, tdd, bdd | lowercase in filenames (`adr-NN-slug.md`); uppercase only for the manuals [[TDD]] / [[BDD]] | files, wikilinks | mixed case in filenames |
| defered | `defered` | `status` token for loose docs/BDDs/TDDs ([[CONVENTION]]); retired for ADRs, which carry no `status` field and are edited in place ([[adr-00-adr-doctrine]] rules 3, 8, REJECTED) | `deferred` — the token's spelling is intentional and machine-checked |
| infrastructure | `INFRASTRUCTURE` (doc name), `infrastructure` (prose) | doc names, wikilinks, prose | `INFRAESTRUCTURE`, `infraestructure` — misspelling, corrected repo-wide |
| localisation (doc) | `LOCALISATION` (doc name, `docs/constitution/LOCALISATION.md`) | doc name, wikilinks | `LOCALIZATION` — American spelling retired repo-wide in favor of harness-default's British form (owner ruling, 2026-08-04); prose still uses either spelling freely, only the doc name and its `[[wikilink]]` are fixed |
| code comments (doc) | `CODE-COMMENTS` (doc name, `docs/CODE-COMMENTS.md`) | doc name, wikilinks | `CODE_COMMENTS`, `COMMENTS`, `comment-style` |
| package manager (frontend) | `bun` | everything JavaScript ([[FRONTEND]]) | `npm` (prohibited), `pnpm`, `yarn` |
| package manager (backend) | `uv` | everything Python ([[BACKEND]]) | `pip`, `poetry` |
| cache | the layers of [[CACHE]] | settings, infra | `redis` (prohibited) |
| fragment | `fragment` | HTMX HTML-over-the-wire responses ([[HTMX]]) | `partial`, `snippet` |
| island | `island` | Svelte interactive components ([[FRONTEND]]) | `widget`, `component` (for this meaning) |
| live-doc block | `live-doc block`, delimited `LIVE-DOC:START … LIVE-DOC:END` | the wikilinks-only region stamped at the top of every matched code file, linking it to the ADRs and docs that govern it ([[adr-19-live-doc-backlinks]], [[HARNESS]]); stamped only by the linker `kdx-live-doc`, never by hand | `docstring header`, `backlink header`, `doclink`, `frontmatter` (for this meaning); any block carrying prose instead of links |
| shadow test | `shadow test` | BDD browser validation ([[BDD]]) | `e2e test`, `smoke test` |
| endpoint | `endpoint` | any route declared in [[API]] | `route` (in API context) |
| project slug | `astro-drf-aws` (this template's reference project; `PROJECT_SLUG` backend/CI, `PUBLIC_PROJECT_SLUG` frontend-derived form) | AWS resource names, secrets paths, hosts ([[INFRASTRUCTURE]]); frontend-visible copy via `PUBLIC_PROJECT_SLUG` ([[VARIABLES]]) | `astro_drf_aws`, `astrodrfaws`; a literal `astro-drf-aws` typed outside the sanctioned single-source points ([[VARIABLES]], issue #133) |
| base domain | `grupoalvs.com` (`BASE_DOMAIN`) | project hosts `<slug>[.dev].grupoalvs.com` | hardcoding the host instead of composing it |
| view | Django server code only — views and viewsets ([[BACKEND]]) | backend code | calling Astro pages or Svelte files "views" |
| page | `page` — an Astro `src/pages/` route ([[FRONTEND]]) | frontend routes | `view` (Django's word), `screen` |
| template | Django template — fragment/HTML rendering ([[BACKEND]], [[HTMX]]) | backend rendering | calling Astro layouts "templates" |
| layout | `layout` — Astro layout component ([[FRONTEND]]) | frontend structure | `template` (Django's word) |
| app (Django) | lowercase, short, domain-named (`users`, `billing`) | Django apps ([[BACKEND]]) | `core`, `utils`, `common` (grab-bags); CamelCase |
| model | singular PascalCase (`User`, `Invoice`) | Django models | plural class names |
| viewset / serializer | `<Model>ViewSet`, `<Model>Serializer` (DRF standard) | DRF classes | `<Model>API`, abbreviations (`<Model>VS`) |
| user identity key | `sub` — the Cognito subject claim, immutable user key | user model, DRF serializers ([[BD]]) | `cognito_id`, `uid`, `user_uuid` |
| guardian | `astro-drf-aws-prd`, `astro-drf-aws-adr`, `astro-drf-aws-api` — SSOT-watching subagents in `agents/` | agent names, dispatch hook, docs | `watcher`, `checker`, ad-hoc renames without a row here |
| vendored skill | a real skill copy under `.claude/skills/<name>/` mirrored to `skills/<name>/`, listed in [[HARNESS]] ([[adr-02-harness]]) | the template's self-contained skill set | `linked skill`, relying on the global `~/.agents/skills/` harness |
| harness document | `docs/constitution/HARNESS.md` | SSOT for the skills the template requires ([[adr-02-harness]]) | `skills.md`, `SKILLS.md`, listing skills in [[AGENTS]] as the source of truth |
| development-loop document | `docs/DEVELOPMENT-LOOP.md` | SSOT of the development loop — the canonical workflows AND their runbook (sequence + tool/skill per step); force is [[adr-11-development-flow]] | `WORKFLOW.md`, `DEV-LOOP.md`, `dev-flow.md`; putting the workflow diagrams inside the ADR; siting the loop's definition in [[PRD]] |
| product requirements document | `docs/constitution/PRD.md` | SSOT of the **objective and the horizon** — the railguard and growth by addition; generalist, top-down, held in context always | `VISION.md`, `SPEC.md`; siting a mechanism, a roadmap, a workflow, or a restated rule in it |
| railguard | the foundations growth may never compromise — stack, infrastructure, and the SSOTs [[PRD]] cites | [[PRD]]; goal-alignment judgements | `guardrail` (the guardians are a different thing — see *guardian*), `rails`, `constraints` |
| componentization | `componentization` | the `.astro`-routes-only / `.svelte`-everything-else discipline ([[adr-08-frontend-and-design-system]] r9, [[COMPONENTIZATION]]) | `componetization` (typo), `component architecture`, `component structure` |
| componentization document | `docs/COMPONENTIZATION.md` | SSOT for the componentization folder tree and rationale ([[adr-08-frontend-and-design-system]] r9) | `COMPONENTS.md`, `ARCHITECTURE.md` |
| vendored MCP server | project-scoped `.mcp.json` + project-local venv under `.mvmcp/`, listed in [[HARNESS]] ([[adr-20-markdown-vault-mcp]]) | the template's self-contained MCP set | relying on a machine-global MCP registration; committing `.mvmcp/` |
| markdown-vault MCP server | `markdown-vault-docs` | the vendored MCP over `docs/`, first source of truth for `docs/` content ([[adr-20-markdown-vault-mcp]], [[markdown-vault-mcp]]) | `markdown-vault-syv` (kodex's SyV instance), `mvmcp` or `vault` as the registered server name |
| markdown-vault env stem | `MARKDOWN_VAULT_MCP_` | launcher env for the vault MCP; harness-dev tooling, not app runtime, never in [[VARIABLES]] ([[adr-20-markdown-vault-mcp]] rule 3) | `MVMCP_`, `VAULT_`, declaring it as an app variable |
| vault MCP profile | `full` / `keyword` | which vault MCP the launcher installs and serves — `full` with semantic search, `keyword` without; selected by `MARKDOWN_VAULT_MCP_PROFILE`, table owned by [[markdown-vault-mcp]] | `embeddings`/`no-embeddings`, `lite`, `degraded`, `offline` as the profile names |
| kdx-bot-token secret env | `KDX_BOT_TOKEN_SECRET_ID` | the AWS Secrets Manager secret id the `kdx-bot-token` Telegram token resolver reads as its durable lane; harness-dev tooling, not app runtime, never in [[VARIABLES]] (precedent [[adr-20-markdown-vault-mcp]] rule 3) | hardcoding a bot token in an `.env` or in code; declaring it an app variable; `TELEGRAM_TOKEN_SECRET`, `KDX_BOT_TOKEN` as the secret-id name |
| inventory document | `docs/INVENTORY.md` | committed record of every created AWS resource ([[INFRASTRUCTURE]], [[adr-15-ephemeral-run]]) | `resources.md`, `assets.md` |
| resource tag keys | `project`, `env`, `lifecycle` | the mandatory AWS tag set on every ephemeral-run resource ([[INFRASTRUCTURE]], [[adr-15-ephemeral-run]]) | capitalized `Project`/`Env`/`Lifecycle`; `environment` as the key; ad-hoc `owner`/`team` keys |
| issue branch | `issue-<n>-<slug>` | git branch that resolves a GitHub issue ([[GH]]); `<n>` = issue number, kebab-case English `<slug>` | `feature/*`, `fix/*`, a bare `<slug>` without the issue number |
| Django project package | `config` | the single Django project/settings package ([[BACKEND]]) | `settings`, `project`, the project slug, or naming it after an app |
| Django app (health) | `health` | health/liveness domain app ([[BACKEND]]); serves `/api/health/` | `healthcheck`, `status`, `ping` |
| Django app (users) | `users` | user/identity domain app ([[BACKEND]], [[AUTH]]); owns the `sub`-keyed user model | `accounts` (that is the URL prefix, not the app), `auth`, `core` |
| RBAC group | `admins` | Django Group for the elevated role ([[AUTH]], [[adr-14-auth]]); RBAC is Django Groups, never Cognito | singular `admin` (collides with Django's `/admin/`), `superusers`, any Cognito group |
| model field (users) | `nickname` | optional user-chosen display name, shown in place of the Cognito-derived given/family name when set ([[API]] `PATCH /api/me/`, [[adr-14-auth]] rule 5) | `display_name`, `alias`, `handle` |
| model field (users) | `avatar_visible` | boolean; when false, the frontend renders two-letter initials instead of `picture` ([[API]]) | `show_avatar` (inverted polarity reads worse at the call site), `hide_avatar` |
| endpoint segment (me) | `me` — `/api/me/` | current-session identity endpoint ([[API]]) | `profile`, `self`, `current-user` |
| endpoint segment (restricted) | `restricted` — `/api/restricted/` | RBAC probe endpoint, group-gated ([[API]]) | `admin-only`, `protected`, `secure` |
| endpoint segment (login) | `login` — `/accounts/login/` | OIDC login kickoff endpoint ([[AUTH]], [[API]]) | `signin`, `log-in`, `auth` |
| bootstrap superuser vars | `DJANGO_SUPERUSER_EMAIL`, `DJANGO_SUPERUSER_PASSWORD` | env vars read by the `bootstrap_admin` management command ([[BACKEND]], [[VARIABLES]]) | `ADMIN_EMAIL`, `ADMIN_PASSWORD`, bare `SUPERUSER_*` (ambiguous with the `admins` RBAC group) |
| endpoint segment (callback) | `callback` — `/accounts/callback/` | OIDC authorization-code redirect target ([[AUTH]], [[API]]) | `cb`, `oauth-callback`, `redirect` |
| endpoint segment (logout) | `logout` — `/accounts/logout/` | session-flush + Cognito-logout endpoint ([[AUTH]], [[API]]) | `signout`, `log-out` |
| endpoint segment (dev-login) | `dev-login` — `/accounts/dev-login/` | DEBUG-only dev login endpoint ([[AUTH]], [[adr-14-auth]], [[API]]) | `devlogin`, `dev_login`, `fake-login`, `test-login` |
| frontend page (showcase) | `showcase` — `/showcase/components/` | Astro SSR visual smoke-test page, vendored shadcn-svelte components ([[FRONTEND]]); frontend-owned, never an [[API]] row; the bare `/showcase/` landing was retired (issue #249), this is the sole surviving showcase route | `demo`, `gallery`, `kitchen-sink`, bare `/showcase/` as a distinct route |
| infra test file | `tests/test_aws_infra.py` | infrastructure assertions + tag audit ([[INFRASTRUCTURE]], [[adr-15-ephemeral-run]]) | `test_infra.py`, `test_aws.py` |
| CI workflow (prod) | `.github/workflows/deploy-prod.yml` | the prod-only deploy workflow ([[INFRASTRUCTURE]], [[adr-15-ephemeral-run]]) | `deploy.yml`, `main.yml`; a `deploy-dev.yml` in this run |
| Cognito user pool | `alvs-prod-astro-drf-aws` | the run's only user pool ([[AUTH]], B2.2) | per-app pools; names without the `alvs-prod-` prefix |
| Cognito hosted-UI prefix | `alvs-astro-drf` | hosted-UI domain prefix ([[AUTH]]); `alvs-astro-drf-aws` was frozen at B1 but rejected by `CreateUserPoolDomain` — `aws` is a reserved word in hosted-UI domain prefixes, so B2.2 fell back to this value | the full pool name as prefix; ad-hoc abbreviations; any prefix containing the word `aws` |
| Cognito test users | `test-admin@grupoalvs.com`, `test-plain@grupoalvs.com` | the B2 RBAC pair; Django group `admins` assigned to the first in C1 fixtures, never in Cognito ([[adr-14-auth]]) | real personal emails; `admin@…`; Cognito groups as roles |
| RDS instance | `alvs-prod-astro-drf-aws-pg` | the dedicated ephemeral instance ([[BD]], [[adr-15-ephemeral-run]] r4) | using shared `alvs-prod-pg` in this run |
| database name | `app` | the SQL database on that instance ([[BD]]) | the hyphenated project slug inside SQL identifiers |
| S3 media bucket | `alvs-astro-drf-aws-media-prod` | private media bucket, no CDN — Django-presigned URLs only ([[INFRASTRUCTURE]]) | public buckets; per-service buckets |
| ECR repositories | `alvs/astro-drf-aws-backend`, `alvs/astro-drf-aws-frontend` | container images, immutable tags `prod-<full-git-sha>` ([[INFRASTRUCTURE]]) | `latest` tags; mutable tags |
| IAM roles (run) | `alvs-prod-astro-drf-aws-<component>-{exec,task}-role`, `<component>` ∈ `backend`, `frontend` | Fargate exec/task roles ([[INFRASTRUCTURE]]); frontend roles get zero secret access ([[adr-14-auth]] r7) | shared roles across projects; inline ad-hoc names |
| target groups | `tg-astro-drf-aws-backend-prod`, `tg-astro-drf-aws-frontend-prod` | shared-ALB attachments ([[INFRASTRUCTURE]]) | names without the `tg-` prefix or `-prod` suffix |
| Cloud Map namespace | `astro-drf-aws-prod.local` | service discovery namespace ([[INFRASTRUCTURE]]) | `.internal`; the bare project slug |
| ECS services | `astro-drf-aws-backend`, `astro-drf-aws-frontend` | the two Fargate services, born at first deploy (D2) ([[INFRASTRUCTURE]]) | `-service` suffixes; per-env suffixes (cluster is already prod) |
| log groups (run) | `/alvs/astro-drf-aws/backend-prod`, `/alvs/astro-drf-aws/frontend-prod` | CloudWatch Logs groups ([[INFRASTRUCTURE]]) | `/ecs/*` defaults |
| secrets paths (run) | `alvs/prod/astro-drf-aws/{django,db,cognito,s3}` | Secrets Manager components; JSON keys per [[VARIABLES]] | SSM Parameter Store; secrets outside this prefix |
| project host | `astro-drf-aws.grupoalvs.com` | ALB A-alias host, no CDN in front ([[INFRASTRUCTURE]]) | pointing the apex or other hosts at this run |
| Django app (m365) | `m365` | app-only Microsoft Graph capability app ([[BACKEND]], [[adr-16-m365-graph]]); no models, owns the two demo Graph read endpoints | `graph`, `microsoft`, `sharepoint` |
| endpoint segment (hello) | `hello` — `/api/m365/hello/` | live Graph read of workbook cell A1, demo endpoint ([[API]], [[adr-16-m365-graph]]) | `cell-a1`, `test-a1` |
| endpoint segment (world) | `world` — `/api/m365/world/` | live Graph read of workbook cell C3, demo endpoint ([[API]], [[adr-16-m365-graph]]) | `cell-c3`, `test-c3` |
| chatbot UI | `chatui` — `/chatui/` | frontend half of the chatbot capability layer — island, page segments, BDD entries; top-level product page, deliberately outside `/showcase/` (owner decision 2026-07-14, issue 101) because it fronts a real API surface while showcase pages never do | `chat-ui`, `chatbox`, `chat-widget`, `chat` as an identifier; `/showcase/chatui` as its route |
| chatbot router | `router` | backend half of the chatbot capability layer — app, model, endpoint and env var stems; distinct from DRF's router classes, which keep their own name | `chatrouter`, `dispatcher`, `classifier` |
| router endpoint segment | `route` — `/api/router/route/` | the phase-1 routing endpoint ([[API]], [[CHATBOT]]) | `intent`, `interpret`, `ask` — the last of those names the assistant's own segment, and naming the router's segment after it would collapse the two tiers into one path |
| router env stem | `ROUTER_` | env vars for the router app, e.g. `ROUTER_ENABLED` ([[VARIABLES]], [[CHATBOT]]) | `CHATBOT_`, `INTENT_`, `AI_` |
| router registry model | `Intent` | the hand-authored menu row: phrase, action target, group gate ([[CHATBOT]]) | `Action`, `Rule`, `IntentRule` |
| router audit model | `IntentQuery` | one row per routed utterance: raw text, menu offered, choice, model id, latency, user ([[CHATBOT]] — Retention) | `RouterLog`, `Query`, `AuditRow` |
| reserved router outcomes | `NO_MATCH`, `ESCALATE` | the two permanent, always-present reserved menu members, distinct from each other and from any registry row ([[CHATBOT]]) | `UNKNOWN`, `FALLBACK`, `NONE`, collapsing either into the other |
| router action field | `kind` | the action-descriptor field encoding reversibility, values `navigate`/`confirm` ([[CHATBOT]]) | `type`, `action_type`, an integer severity/level |
| cooldown | `cooldown`, class `CooldownThrottle` (`backend/config/throttling.py`) | per-user throttle at rate `1/<period>` on expensive endpoints, built on DRF's throttling + the shared `DatabaseCache` ([[CACHE]], [[BACKEND]]) | `rate limit`, `rate-limit` as the feature name; a bespoke, hand-rolled implementation |
| router operator group | `ai_operators`, Django `Group` | the second RBAC group (alongside `admins`) admitted to `POST /api/router/route/` ([[API]], [[adr-14-auth]]); router-only — never added to any other DRF permission class | `operators`, `router_users`, `chatbot_users` |
| router permission class | `CanUseRouter` (`backend/apps/router/permissions.py`) | DRF permission gating `RouteView`: authenticated AND member of `admins` or `ai_operators` ([[API]]) | `IsRouterUser`, reusing `IsInAdminsGroup` unmodified for the router |
| router rate-abuse guard | `evaluate_rate_abuse`, `is_rate_blocked` (`backend/apps/router/rate_abuse.py`) | async, DB-cache-backed silent block on the router's `POST /api/router/route/` when a user's average messages-per-minute crosses a threshold (#371, [[API]], [[adr-18-async-mandatory]], [[adr-10-cache]]) — enforcement only, never authorization | `RateLimiter`, `RouterThrottle` (collides with `CooldownThrottle`'s naming axis) |
| generating-tier surface | `assistant` | the page-context assistant — the generating tier's first sanctioned surface: app, endpoint, model and env var stems, and the `ChatUI` mode name; read-only forever, never an actuator — context is server-assembled from page identity and links are validated against the closed nav registry ([[CHATBOT]], [[adr-25-page-context-assistant]]) | `chatbot` as this surface's identifier, `ai`, `copilot`, `agent`, `brain`; reusing `router` for it (the two tiers are disjoint, so their names are too) |
| assistant endpoint segment | `ask` — `/api/assistant/ask/` | the generating tier's direct channel ([[API]], [[CHATBOT]], [[adr-25-page-context-assistant]]) | `query`, `chat`, `prompt`, `answer`; `route` (that is the choosing tier's segment) |
| assistant drawer component | `ChatDrawer` | the drawer composition mounting `ChatUI` (assistant mode) inside `overlay/Drawer` on every page, on the opposite edge of `NavDrawer` — a composition, not a widget, and never a second chat component ([[COMPONENTIZATION]], [[adr-25-page-context-assistant]]) | `ChatPanel`, `AssistantDrawer`, `ChatSidebar`; a forked copy of `ChatUI` for the drawer |
| assistant env stem | `ASSISTANT_` | env vars for the assistant app, e.g. `ASSISTANT_ENABLED` ([[VARIABLES]], [[CHATBOT]]); each row enters [[VARIABLES]] before any code reads it ([[adr-07-api-and-backend]] rule 7) | `ROUTER_` (that stem is the choosing tier's), `AI_`, `CHATBOT_`, `LLM_` |
| assistant permission class | `CanUseAssistant` (`backend/apps/assistant/permissions.py`) | DRF permission gating `AskView`: authenticated AND member of `admins` or `ai_operators` ([[API]], [[adr-14-auth]]) | `IsAssistantUser`, reusing `CanUseRouter` unmodified for the assistant |
| library | Melt UI (pkg `melt`) | headless builder layer under Bits UI/shadcn-svelte ([[MELT-UI]]) | `@melt-ui/svelte` (legacy pkg), MeltUI |
| component layering posture | `posture` | this template's standing default for which component layer to reach for first — Melt builder, then shadcn-svelte, then custom ([[MELT-UI]], [[DESIGN-SYSTEM]], [[adr-08-frontend-and-design-system]] r8) | `priority`, `preference`, `strategy` for this meaning |
| model field (users) | `theme_config` | per-user appearance blob `{mode,bgPreset,sidebarSide,colors,radius}`, mirrored to the `theme` cookie ([[API]], [[DESIGN-SYSTEM]]) | `theme_settings`, `prefs`, `appearance_config` |
| cookie | `theme` | non-HttpOnly cookie mirroring `theme_config` for no-flash SSR ([[DESIGN-SYSTEM]], [[CACHE]]) | `theme_cookie` |
| localStorage key | `shell-nav-pinned` | persists `NavDrawer`'s pin preference across sessions, read on mount ([[COMPONENTIZATION]]) | `nav_pinned`, `navPinned`, `sidebar-pinned` |
| theme value | `mode` (`light`\|`dark`) | appearance mode ([[DESIGN-SYSTEM]]) | `dark_mode`, `color_scheme` |
| theme value | `bgPreset` (`default`\|`melt`) | background preset; `melt` = amber dotted field ([[DESIGN-SYSTEM]]) | `background_style`, `wallpaper` |
| theme value | `sidebarSide` (`left`\|`right`) | which viewport edge the `NavDrawer` docks to; the `ChatDrawer` always takes the opposite one ([[DESIGN-SYSTEM]]) | `sidebar_position`, `drawer_side`, `nav_side` |
| theme color key | `canvas` | the page field — the `melt` preset's base, the large dark/light expanse (`theme.ts`, [[DESIGN-SYSTEM]]) | `bg`, `page_bg` |
| theme color key | `dots` | the dots drawn over `canvas` under the `melt` preset (`theme.ts`, [[DESIGN-SYSTEM]]) | `melt_dots`, `dot_color` |
| theme color key | `surface` | control and card fill — what `bg-background` resolves to; replaces the retired `background` key (`theme.ts`, [[API]]) | `background` (retired v1 name), `card_bg` |
| theme color key | `foreground` | text color over `surface` (`theme.ts`, [[DESIGN-SYSTEM]]) | `text`, `fg` |
| model field (users) | `theme_config.colors` | mode-keyed palette map `{light?, dark?}`, each a `ThemeColors` object of the seven color keys above ([[API]] v2, [[DESIGN-SYSTEM]]) — a flat, non-mode-keyed `colors` is the retired v1 shape, read only through `theme.ts`'s legacy fallback | `palette`, `theme_colors` |
| theme pack | `ThemePackId` (`kodexarg`\|`jeremias`\|`militar`\|`amber`) | a curated, client-side-only dual-mode color collection composed onto the current `theme_config` draft via `themeConfigFromPack()`; never persisted as an id, only its resulting `colors` blob is ([[DESIGN-SYSTEM]], `theme-packs.ts`) | `theme_preset`, `collection_id` |
| Appearance card section | `Colecciones` (i18n `appearance_packs`) | the theme-pack picker row on the `/profile` Appearance card ([[DESIGN-SYSTEM]]) | `Presets`, `Temas`, `Kits` |
| color token (financial) | `--success` / `--success-foreground` | positive financial state, distinct from any UI success/validation meaning ([[DESIGN-SYSTEM]]) | `--positive`, `--green` |
| color token (financial) | `--warning` / `--warning-foreground` | equilibrium/neutral financial state ([[DESIGN-SYSTEM]]) | `--neutral-state`, `--yellow` |
| color token (financial) | `--negative` / `--negative-foreground` | danger financial state — a separate token name from `--destructive` even where the value is shared ([[DESIGN-SYSTEM]]) | `--danger`, `--red`, reusing `--destructive` directly instead of the named token |
| model (access) | `AccessRequest` | Django model for an admin-managed role-grant record, one per user ([[AUTH]], [[adr-21-authorization-lobby]]) | `Grant`, `RoleRequest`, `AccessGrant` |
| model field (access) | `role` | nullable `ForeignKey` to Django's own `Group` on `AccessRequest`; `null` = pending/unassigned — RBAC roles ARE Django Groups, never a parallel role table ([[adr-21-authorization-lobby]], [[adr-14-auth]] rule 2) | `group`, `assigned_role`, `role_name` |
| UX term (lobby) | `lobby` | the route `/`, admitting BOTH an anonymous visitor and a role-less authenticated session — the only page reachable before a role is granted ([[adr-21-authorization-lobby]]) | `waiting room`, `landing`, `landing page` |
| env var (allowlist) | `AUTH_BOOTSTRAP_ALLOWLIST` | comma-separated `email:group` pairs pre-authorizing accounts at first login — group omitted defaults to `admins`; the operator-set counterpart of the admin grant ([[adr-22-bootstrap-allowlist-grant]], [[AUTH]], [[VARIABLES]]) | `AUTH_ALLOWLIST`, `PREAUTHORIZED_USERS`, `AUTO_GRANT_EMAILS`, baking accounts into code |
| message key (lobby) | `lobby_pending` | English i18n key for the lobby's pending-authorization legend; the rendered value is Spanish, owned by the i18n layer ([[LOCALISATION]], [[adr-21-authorization-lobby]]) | hardcoding the Spanish string in code, a non-English key |
| i18n module | `frontend/src/i18n/` | the frontend's translation-catalog SSOT — locale config, per-locale catalog, render helper ([[LOCALISATION]], [[FRONTEND]]) | `frontend/src/locales/`, `frontend/src/lang/`; a per-page translation mechanism |
| default locale | `es` | this reference run's default locale, set in `frontend/src/i18n/config.ts` ([[LOCALISATION]]) | `en` as this run's shipped default; a locale hardcoded outside `config.ts`; region tags (`es-AR`, `es_ES`) for it |
| render helper | `t` | the localization function, signature `t(key, locale?)` → localized string ([[LOCALISATION]], [[FRONTEND]]) | `translate`, `i18n`, `__`, `gettext` as the helper name |
| message ID | English `snake_case` (e.g. `lobby_pending`) | the catalog key; IDs/keys/vars stay English, only the rendered value localizes ([[adr-05-glossary-and-localisation]], [[LOCALISATION]]) | non-English keys; camelCase/kebab-case IDs; the localized string used as its own key |
| locale | `locale` — a language code (e.g. `es`, `en`) | the language selector for routing and catalog lookup ([[LOCALISATION]]) | `lang`, `language`, `i18n` for this meaning |
| catalog | `catalog` | a per-locale map of message ID → localized string ([[LOCALISATION]]) | `messages`, `translations`, `dictionary`, `strings` for this meaning |
| showcase component (select) | `Select` | Tier-1 single-select popover component, `form/`, Melt Select builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `Dropdown`, `SelectBox`, `select-input` |
| showcase component (combobox) | `Combobox` | Tier-1 searchable single-select component, `form/`, Melt Combobox builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `SearchSelect`, `Autocomplete` |
| showcase component (checkbox) | `Checkbox` | Tier-1 boolean checkbox component, `form/`, Melt Toggle builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `CheckBox`, `Check` |
| showcase component (switch) | `Switch` | Tier-1 boolean pill/thumb switch component, `form/`, Melt Toggle builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `Toggle` (reserved for the theme mode control), `ToggleSwitch` |
| showcase component (date picker) | `DatePicker` | Tier-1 single-date filter component, `form/`, Melt Popover over a native `<input type="date">` ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `DateField`, `date-picker` as the code identifier |
| showcase component (date range picker) | `DateRangePicker` | Tier-1 from/to date-range filter component, `form/`, Melt Popover over two native `<input type="date">` ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `DateRangeField`, `RangePicker` |
| showcase component (tabs) | `Tabs` | Tier-1 tabbed navigation component, `nav/`, Melt Tabs builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `TabGroup`, `TabBar` |
| showcase component (toast) | `Toast` | Tier-1 toast notification component, `feedback/`, Melt Toaster builder; its module-level singleton instance is `toaster` ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `Notification`, `Snackbar`, `Toaster` as the component name (that is the singleton instance, not the component) |
| showcase component (tooltip) | `Tooltip` | Tier-1 hover/focus hint component, `overlay/`, Melt Tooltip builder ([[COMPONENTIZATION]], [[MELT-UI]], issue #178) | `Hint`, `Popover` (reserved for the distinct Melt Popover builder) |
| showcase component (dropdown menu) | `Dropdown Menu` (prose), `DropdownMenu` (code identifier) | Tier-2 nav component — user menu, row actions — `nav/`, Melt Popover primitive + hand-rolled roving-focus/typeahead menu semantics (no Melt Dropdown Menu builder in melt 0.44) ([[COMPONENTIZATION]], [[MELT-UI]], [[bdd-09-dropdown-menu-showcase]], issue #178) | `DropdownMenu` in prose without the space, `ContextMenu`, `ActionMenu` |
| showcase component (popover) | `Popover` | Tier-2 overlay component, `overlay/`, Melt Popover builder used bare (float/anchor/dismiss shell, no menu semantics) — the exact reservation made at the `Tooltip` row above (COMPONENTIZATION, MELT-UI, issue #213) | `ContextMenu`, `Flyout` |
| showcase component (pagination) | `Pagination` | Tier-2 data component, `data/`, hand-rolled prev/next + numbered page controls (no Melt Pagination builder in melt 0.44, same absence recorded at the `Dropdown Menu` row above); pairs with `DataTable` ([[COMPONENTIZATION]], [[MELT-UI]], issue #219) | `Pager`, `PageNav` |
| showcase component (PIN Input) | `PIN Input` (prose), `PinInput` (code identifier) | Tier-2 form component, `form/`, Melt PinInput builder — a second-factor confirmation-code field ([[COMPONENTIZATION]], [[MELT-UI]], issue #221) | `Otp`, `OtpInput`, `PinCode` |
| showcase component (Tags Input) | `Tags Input` (prose), `TagsInput` (code identifier) | Tier-2 form component, `form/`, hand-rolled (no Melt TagsInput builder in melt 0.44, same absence recorded at the `Dropdown Menu`/`Pagination` rows above) — multi-filter/recipients chip entry ([[COMPONENTIZATION]], [[MELT-UI]], issue #223) | `Chips`, `MultiInput`, `TokenField` |
| showcase component (Scroll Area) | `Scroll Area` (prose), `ScrollArea` (code identifier) | Tier-2 overlay component, `overlay/`, hand-rolled (no Melt ScrollArea builder in melt 0.44, same absence recorded at the `Dropdown Menu`/`Pagination`/`Tags Input` rows above) — constrained-height scroll container with a themed scrollbar ([[COMPONENTIZATION]], [[MELT-UI]], issue #227) | `Scrollable`, `ScrollBox`, `ScrollPane` |
| showcase component (Calendar) | `Calendar` | Tier-2 form component, `form/`, hand-rolled inline single-month grid (no Melt Calendar builder in melt 0.44, same absence recorded at the `Dropdown Menu`/`Pagination`/`Tags Input`/`Scroll Area` rows above) — single-date selection, the inline-view sibling of `DatePicker` ([[COMPONENTIZATION]], [[MELT-UI]], issue #229) | `DateCalendar`, `MonthView` |
| showcase component (Range Calendar) | `Range Calendar` (prose), `RangeCalendar` (code identifier) | Tier-2 form component, `form/`, hand-rolled inline single-month grid (no Melt RangeCalendar builder in melt 0.44, same absence recorded at the `Calendar` row above) — from/to range selection, the inline-view sibling of `DateRangePicker` ([[COMPONENTIZATION]], [[MELT-UI]], issue #229) | `DateRangeCalendar`, `RangeView` |
| showcase component (Slider) | `Slider` | Tier-3 form component, `form/`, Melt Slider builder — a numeric range control ([[COMPONENTIZATION]], [[MELT-UI]], issue #231) | `Range`, `RangeInput` |
| showcase component (Toggle Group) | `Toggle Group` (prose), `ToggleGroup` (code identifier) | Tier-3 form component, `form/`, composed of Melt Toggle builder instances (no dedicated Melt ToggleGroup builder in melt 0.44, same absence-with-composition shape as the `Dropdown Menu`/`Pagination`/`Tags Input`/`Scroll Area`/`Calendar`/`Range Calendar` rows above) — single-select segmented option group ([[COMPONENTIZATION]], [[MELT-UI]], issue #231) | `SegmentedControl`, `ButtonGroup` |
| showcase component (Context Menu) | `Context Menu` (prose), `ContextMenu` (code identifier) | Tier-3 nav component, `nav/`, Melt Popover + hand-rolled roving-focus/typeahead menu semantics (no Melt Context Menu builder in melt 0.44, same absence recorded at the `Dropdown Menu` row above), opened at the pointer on `contextmenu` ([[COMPONENTIZATION]], [[MELT-UI]], issue #233) | `RightClickMenu`, `ActionMenu` (reserved for the `Dropdown Menu` distinction) |
| showcase component (Menubar) | `Menubar` | Tier-3 nav component, `nav/`, hand-rolled composition of `Dropdown Menu`-style Melt Popover menus in a row with cross-trigger roving focus (no dedicated Melt Menubar builder in melt 0.44, same absence-with-composition shape as the `Toggle Group` row above) ([[COMPONENTIZATION]], [[MELT-UI]], issue #233) | `MenuBar`, `TopMenu`, `AppMenu` |
| showcase component (Table of Contents) | `Table of Contents` (prose), `TableOfContents` (code identifier) | Tier-3 nav component, `nav/`, hand-rolled plain anchor-link list with `IntersectionObserver` active-section highlighting (no Melt Table of Contents builder in melt 0.44 — not a menu, deliberately no roving-focus/typeahead semantics) ([[COMPONENTIZATION]], [[MELT-UI]], issue #233) | `TOC` as the code identifier, `SideNav`, `PageOutline` |
| showcase component (Hover Card) | `Hover Card` (prose), `HoverCard` (code identifier) | Tier-3 overlay component, `overlay/`, Melt Popover + hand-rolled hover/focus open-close semantics (no dedicated Melt Hover Card builder in melt 0.44, same absence-with-composition shape as the `Dropdown Menu` row above) — a hover-triggered rich preview, never opened by click ([[COMPONENTIZATION]], [[MELT-UI]], issue #235) | `LinkPreview`, `Popover` (reserved for the distinct bare Melt Popover use) |
| showcase component (Collapsible) | `Collapsible` | Tier-3 data component, `data/`, Melt Collapsible builder — a single expand/collapse disclosure, generalizing the per-row expand `DataTable` already does ([[COMPONENTIZATION]], [[MELT-UI]], issue #235) | `Disclosure`, `Expander` |
| showcase component (Tree) | `Tree` | Tier-3 data component, `data/`, Melt Tree builder, self-recursive via `<svelte:self>` — a hierarchical expandable/selectable tree for holding/intercompany account hierarchies ([[COMPONENTIZATION]], [[MELT-UI]], issue #235) | `TreeView`, `NestedList` |
| showcase component (SidePanel) | `SidePanel` | **RETIRED** — superseded by the `NavDrawer`/`ChatDrawer` shell (rows above and below); the component is removed and the name stays forbidden. Historical row: Overlay component, `overlay/`, hand-rolled — a persistent, viewport-docked, collapsible panel (`side` left/right) that slides its full width off-screen leaving only a peek tab at the edge; no Melt builder (layout state, not a float/dismiss overlay), distinct from `Drawer` (modal slide-in) ([[COMPONENTIZATION]], [[MELT-UI]]) | `Sidebar`, `Rail`, `Panel` (bare), `SideDrawer` |
| showcase component (HomeTriangle) | `HomeTriangle` | **RETIRED** — superseded by `NavDrawer` (home is a `NAV_ITEMS` entry); the component is removed and the name stays forbidden. Historical row: Zero-hydration-capable primitive, `primitives/`, hand-rolled — a small triangle fixed at the viewport's top-left corner holding an inline home-icon SVG; a persistent "go home" affordance, exploratory spike (issue #255, [[adr-23-showcase-ready-components]]) | `CornerHome`, `TriangleHomeButton` |
| shell component (NavDrawer) | `NavDrawer` | `shell/`, the site nav drawer composed by `Base.astro` on the viewport edge the theme field `sidebarSide` names (default left) — items from the single `NAV_ITEMS` registry (`frontend/src/lib/components/shell/nav.ts`), never a hardcoded list ([[COMPONENTIZATION]], [[DESIGN-SYSTEM]]) | `Sidebar`, `SideNav`, `NavRail`; a second nav-items source outside `shell/nav.ts` |
| component (HomeCardsView) | `HomeCardsView` | `views/`, the `/` page body — home cards from `NAV_ITEMS` plus the denied/pending lobby surfaces; replaces the retired `LobbyView` ([[COMPONENTIZATION]], [[adr-21-authorization-lobby]]) | `LobbyView` (retired name), `HomeView`, `LobbyCardsView` |
| nav registry | `NAV_ITEMS` (`frontend/src/lib/components/shell/nav.ts`) | the single registry of the site's routes — nav drawer, home cards, and the assistant's page identity all read this list and nothing else; holds exactly `/chatui/`, `/showcase/components/`, `/profile/` (`/` is `HomeCardsView`'s own route, not a nav item) ([[COMPONENTIZATION]]) | a second nav-items source outside `shell/nav.ts`; calling it a component |
| nav registry (grouped) | `NAV_SECTIONS` (`frontend/src/lib/components/shell/nav.ts`) | the section-grouped sibling view over `NAV_ITEMS` that `NavDrawer` renders — same single source, never a second authority ([[COMPONENTIZATION]]) | a second nav-items source outside `shell/nav.ts`; a hand-kept parallel list in `NavDrawer` |
| shell component (NavItem) | `NavItem` | `shell/`, one `NAV_ITEMS` entry rendered as a nav anchor — icon, label, and `NavBadge` over the project's own `ui/button` (`secondary` when active, `ghost` otherwise); the active state arrives from the caller, never read from `window` ([[COMPONENTIZATION]], [[DESIGN-SYSTEM]]) | `NavLink`, `DrawerItem` |
| shell component (NavBadge) | `NavBadge` | `shell/`, the pending-count pill inside a `NavItem`; renders nothing when its count is zero or unknown ([[COMPONENTIZATION]]) | `NavCount`, `Badge` (bare, for this meaning) |
| shell component (NavbarIcon) | `NavbarIcon` | `shell/`, the shared chrome for every navbar icon control — the consumers own the glyph and the behaviour; a bare mount renders a placeholder dot, never throws ([[DESIGN-SYSTEM]], [[adr-23-showcase-ready-components]]) | `NavIcon`, `IconButton` |
| header component (LayoutHeader) | `LayoutHeader` | `header/`, the layout-owned header keeping the `banner` landmark as a sibling of `<main>`, never a child; composes one `NavBar` and authors no chrome of its own ([[COMPONENTIZATION]]) | `Header` (bare), `SiteHeader`, `AppHeader` |
| header component (NavBar) | `NavBar` | `header/`, the header's chrome — one `rounded-full` bar carrying the page's single `<h1>` and the session island through an `actions` snippet; renders a `<div>`, never a `<nav>` ([[COMPONENTIZATION]], [[DESIGN-SYSTEM]]) | `TopBar`, `HeaderBar`; a `<nav>` element for it (it carries no navigation) |
| primitive (PageCanvas) | `PageCanvas` | `primitives/`, the page's one `<main>` landmark, composed by `Base.astro` — `min-h-dvh`, full width, transparent, clearing the header; no measure of its own ([[COMPONENTIZATION]]) | `Main`, `PageMain`, `Canvas` (bare) |
| primitive (Surface) | `Surface` | `primitives/`, the named surface primitive owning only the chrome — border, radius, background/foreground pair, padding, shadow — of a raised or contained region through its `level` variants; layout classes stay the caller's ([[DESIGN-SYSTEM]], [[COMPONENTIZATION]]) | `Panel` (bare), `Box`, `Card` as the primitive (the `ui/card` composition keeps its own name) |
| primitive (PageHeading) | `PageHeading` | `primitives/titles/`, a view-level `<h1>` heading with an optional help `Tooltip` ([[COMPONENTIZATION]]) | `PageTitle` as this component's name (that is its sibling in `titles/`), `Heading` (bare) |
| component (HomeCard) | `HomeCard` | `views/`, one row of the home's card list — icon, title and abstract wrapped in a single navigating `<a>`, purely presentational, zero-prop-safe ([[COMPONENTIZATION]], [[adr-23-showcase-ready-components]]) | `NavCard`, `LobbyCard` |
| triage-and-fix cast | `kwf-*` (`docs/agents/kwf-*.md`) | the canonical, runtime-agnostic node roster for the triage-and-fix party ([[adr-04-issue-delivery]] rule 7, `docs/skills/triage-and-fix/`) | `wf-*` for this cast (that prefix names the pre-existing Claude Workflow rendering only) |
| triage-and-fix node names | `hunter`, `falcon`, `hound`, `mage`, `sorcerer`, `owl`, `cat`, `mouse`, `inquisitor`, `warrior`, `thief`, `dwarf`, `archer`, `elf-mage`, `paladin`, `priest`, `shadow`, `bard` | the fixed roster of `kwf-*` nodes, one per phase role (`docs/skills/triage-and-fix/references/cast.md`) | renaming a node ad hoc without updating `cast.md` first |
| triage-and-fix skill | `triage-and-fix` (`docs/skills/triage-and-fix/`) | the canonical runtime-agnostic SSOT for the `kwf-*` cast and playbook ([[adr-04-issue-delivery]] rule 7, [[HARNESS]]) | confusing it with `kdx-wf-triage-and-fix` (the Claude Workflow rendering of the same shape) |
| assertion | `assertion` (`docs/assertions/`) | an owner-reserved law, proved by tests linked from its own body ([[adr-01-constitution]] rule 7, [[assertion-00-discipline]]) | treating an assertion as equal-or-above the constitution/ADRs in authority |
| assertion-review skill | `assertion-review` (`docs/skills/assertion-review/`) | enforces the `docs/assertions/` law family — every assertion's proving tests actually prove it ([[adr-01-constitution]] rule 7, [[HARNESS]]) | `assertion-check`, `law-review` |
| cross-runtime dispatch document | `docs/RUNTIMES.md` ([[RUNTIMES]]) | SSOT for cross-runtime dispatch mechanics and model-tier mapping of the `kwf-*` cast ([[adr-04-issue-delivery]] rule 7) | restating tier mappings inside `cast.md` or an ADR |
| guardian dispatch script | `guardian-dispatch` (`docs/hooks/guardian-dispatch`) | the runtime-agnostic dispatch script reading a guardian's own `watch:` frontmatter, wrapped by `.claude/hooks/dispatch_guardians.py` ([[adr-03-guardians]] rule 8, [[HARNESS]]) | a second hand-kept watchlist copy |
| soul | `soul` (`docs/agents/souls/kwf-*.md`) | the per-node prose/fiction file a `kwf-*` definition points at via its `soul:` frontmatter key — a render, never an input (`docs/skills/triage-and-fix/references/cast.md` rule 1) | inlining a node's fiction into its own frontmatter file instead of its `soul:` file |
